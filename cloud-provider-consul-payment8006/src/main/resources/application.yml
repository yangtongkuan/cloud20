server:
  port: 8006
spring:
  application:
    name: cloud-consul-payment
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      host: localhost
      port: 8500


